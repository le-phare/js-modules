# jquery.scrollToAnchor.js

Super simple plugin to make a link scroll to the anchor with configurable headerHeight.

## Example

```js
$('a[href^="#"]').scrollToAnchor({
    'headerHeight': 100
});
```
